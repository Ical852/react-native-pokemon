import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  image: {
    width: '100%',
    height: 225,
    borderRadius: 8,
  },
  title: {
    fontSize: 20,
    marginTop: 10,
    color: '#000',
  },
});